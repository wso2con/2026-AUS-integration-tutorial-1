// Configurable values
configurable string kafkaBootstrapServers = ?;
configurable string kafkaTopic = ?;
configurable string kafkaGroupId = ?;
configurable string customerNotificationEndpoint = ?;
configurable string posSystemEndpoint = ?;
configurable string analyticsEndpoint = ?;
